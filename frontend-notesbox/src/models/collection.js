export default class Collection {
    id;
    displayName;
    products;
  
    constructor(id, displayName, products) {
      this.id = id;
      this.displayName = displayName;
      this.products = products;
    }
  }
  